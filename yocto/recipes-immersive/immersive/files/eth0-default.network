# Wired config for every node: DHCP. Addresses come from the site network;
# nodes are found by mDNS (<node>.local). No static IPs, no DHCP server of our
# own. systemd-networkd only — no NetworkManager, no connman.
[Match]
Name=eth0

[Network]
DHCP=ipv4
